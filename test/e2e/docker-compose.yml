version: '3'
services:
  webapp1:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8080:80"
    container_name: reserve_proxy

  webapp2:
    image: bullet_client:latest
    container_name: bullet_client
